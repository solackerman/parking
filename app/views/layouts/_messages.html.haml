- flash.each do |name, msg|
  %div{:class => "alert alert-#{name == :success ? "success" : name == :error ? "error" : name == :info ? "info" : "block"}"}
    %a.close{"data-dismiss" => "alert"} &#215;
    = content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String)